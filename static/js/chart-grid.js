// chart-grid.js

/**
 * Временная заглушка computeGrid/getTimeTicks/getPriceTicks:
 * старая логика сетки и тиков отключена.
 */

export function computeGrid(layout, settings) {
  return null;
}

export function getTimeTicks(layout) {
  return [];
}

export function getPriceTicks(layout) {
  return [];
}
